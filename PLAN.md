# RAG 服务公司 · 计划书

> 目标：为其他企业提供"嵌入其现有系统的智能问答窗口"服务。
> 本文件夹为全部开发工作区。

---

## 0. 先说结论

这个生意能做，但**先别想着"给所有公司做"**。正确路径是：

1. 用你自家的光伏公司做**第一个真实客户**（免费，但真实）
2. 跑通"企业文档 → 问答窗口嵌入客户系统"全流程
3. 打磨成可复用的产品骨架
4. 再向外找客户

你现在有 >100 万存款、不缺钱，缺的是**一个能交付的东西**和**第一个愿意付钱的人**。

---

## 1. 你到底在卖什么

不是"RAG技术"。客户不关心你用什么向量数据库、什么 embedding 模型。

你卖的是：**"花 X 万，在你的客服系统/ERP/后台里加一个能回答客户问题的AI窗口，不用培训你的员工学新技术。"**

客户画像（按优先级）：
| 客户类型 | 痛点 | 付费意愿 | 难度 |
|---------|------|---------|------|
| 制造业（光伏/新能源） | 产品手册/售后知识多，客服答不全 | 中 | 低（你有行业认知） |
| 电商/零售 | 商品知识库、售后问答 | 中 | 低 |
| 法律/医疗 | 专业文档问答 | 高 | 高（合规） |
| 通用企业 | 内部知识库 | 中 | 中 |

**建议起点：光伏/新能源行业，从你自己的公司开始。**

---

## 2. 产品形态

客户要的是"嵌进去"，不是"再登录一个系统"。产品分三层：

```
┌─────────────────────────────────────────┐
│  客户现有系统 (ERP/客服后台/官网)          │
│  ┌───────────────────────────────────┐  │
│  │  AI 问答窗口 (你的产品)              │  │  ← 嵌入层：iframe / JS SDK
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
         │
         │ API 通信
         ▼
┌─────────────────────────────────────────┐
│  RAG 服务端 (你的核心)                    │
│  • 文档入库 / 向量化                      │
│  • 检索 (向量库)                         │
│  • 生成回答 (LLM)                        │
│  • 多租户隔离                            │
└─────────────────────────────────────────┘
```

**嵌入方式（核心决策）：**
- **方案A：iframe 嵌入** — 最简单，客户加一行 HTML 即可。用 postMessage 通信。MVP 用这个。
- **方案B：JS SDK** — 更灵活，能深度集成。二期再做。
- **方案C：纯 API** — 客户自己写前端。留给技术型客户。

MVP 选 **方案A**。

---

## 3. 技术栈（结合你现有能力）

你有：Python（会做RAG）、Node.js+Express+React+antd、Ollama本地LLM、RTX 3060 12GB、Docker（要学）。

| 层 | 技术选择 | 理由 |
|----|---------|------|
| **RAG 后端** | Python + FastAPI | 比 Flask 更适合生产，异步、自动文档 |
| **文档解析** | pypdf / python-docx / unstructured | PDF/Word/Excel/Markdown 全覆盖 |
| **文本分块** | 自研 sliding-window | 简单可控 |
| **Embedding** | bge-large-zh（本地） | 中文效果最好，本地跑，无 API 费用 |
| **向量数据库** | ChromaDB（起步）→ Milvus（生产） | Chroma 零配置，Milvus 分布式 |
| **LLM** | Ollama（qwen2.5:7b，本地）/ 客户可选 API | 本地隐私好，API 效果好 |
| **问答窗口前端** | React + 独立窗口组件 | 你熟 antd，但窗口要轻量独立 |
| **嵌入通信** | iframe + postMessage | MVP 最简单 |
| **多租户** | 客户ID 隔离向量空间 + 数据 | 关键！不同客户数据绝不能串 |
| **部署** | Docker Compose | 一键部署到客户服务器 |
| **配置管理** | .env + YAML | 你熟 dotenv |

**不选的东西：**
- ❌ LangChain/LlamaIndex（MVP 阶段太重，先自己写清逻辑）
- ❌ Kubernetes（一个人不需要）
- ❌ 微服务架构（过早复杂化）

---

## 4. 你还要学的东西（按优先级）

| 要学的 | 为什么 | 怎么学 |
|--------|--------|--------|
| **Docker** | 交付给客户必须容器化 | 官方 Quickstart，2小时 |
| **FastAPI** | RAG 后端框架 | 官方教程，1天 |
| **bge 模型本地部署** | 中文 embedding | sentence-transformers 一行代码 |
| **iframe postMessage 通信** | 嵌入核心 | MDN + 写个 demo |
| **多租户架构设计** | 商业化的命门 | 设计文档 + 实现 |
| **B2B 销售话术** | 没有销售=没有收入 | 看你自己的光伏业务怎么谈客户 |
| **报价与合同** | 怎么定价、签什么 | 找模板+律师过一遍 |

---

## 5. 里程碑

### 阶段一：MVP（2-3周）
- [ ] 搭 RAG 后端：文档入库 → 向量化 → 检索 → 回答（Python + FastAPI + Chroma + bge）
- [ ] 本地 Ollama 接入（qwen2.5:7b）
- [ ] 做一个可嵌入的问答窗口（React + iframe demo）
- [ ] **用光伏公司真实文档跑通**：产品手册、售后FAQ、公司制度

### 阶段二：产品化（3-4周）
- [ ] 多租户隔离架构
- [ ] 管理后台：客户管理、文档管理、对话记录（你熟 antd）
- [ ] Docker Compose 一键部署
- [ ] 客户文档上传 + 自动向量化流程
- [ ] 嵌入 SDK（iframe + postMessage 封装）

### 阶段三：验证（持续）
- [ ] 在光伏公司内部用 1 个月，收集反馈
- [ ] 改造成"给另一家光伏公司部署"的演示版
- [ ] 找 1-2 家试点客户（免费/低价换反馈）

### 阶段四：商业化
- [ ] 定价：按坐席数？按查询量？按年费？
- [ ] 标准合同 + SLA
- [ ] 官网 + 案例展示
- [ ] 正式获客

---

## 6. 商业模式（现在就想清楚）

| 收费方式 | 优点 | 缺点 | 适合 |
|---------|------|------|------|
| 一次性部署费 | 简单 | 无持续收入 | 私有化部署客户 |
| 年费/订阅 | 持续收入 | 客户流失风险 | SaaS模式 |
| 按查询量 | 公平 | 收入不稳定 | 大客户 |
| 部署费+年费 | 兼顾 | 复杂 | **推荐** |

**建议定价参考：**
- 小型企业：部署 ¥1-3万 + 年费 ¥1-2万
- 中型企业：部署 ¥5-10万 + 年费 ¥3-5万
- （等你有了案例再调）

---

## 7. 现在就开始的行动（本周）

1. **读一遍这个计划**，标记你不认同的点
2. **列出光伏公司已有的文档**（产品手册、培训材料、FAQ），算算有多少份、多大
3. **决定阶段一的技术栈细节**，我陪你搭
4. **开始写代码**——不要等"想清楚了再做"

---

## 8. 我要刺你的一句话

你这个计划最大的风险不是技术，是**你写完了这个 PLAN.md 然后就再也没打开过它**。

你之前：
- 电商小程序：技术做完了，零收入，"找不到买家"
- 光伏 AI 短视频：承认是伪需求
- 各种博客：写完了，没带来客户

**这次唯一的区别是：你有家真实的公司可以做第一个客户。**

别再分析瘫痪了。本周要不要动手搭 RAG 后端？
